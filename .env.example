DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=
MASTER_DB_NAME=visual_admin_db

# Opcional: ruta URL del directorio de public (sin barra final), ej. /visual/public
# Si se omite, se usa el directorio de index.php (auto).
APP_BASE_PATH=

# 1 en producción HTTPS
SESSION_COOKIE_SECURE=0
